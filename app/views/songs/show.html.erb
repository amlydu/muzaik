<p>
  <strong>Name:</strong>
  <%= @song.name %>
</p>
<p>
  Rating: <%= rating_for @song, "overall", :enable_half => true, :half_show => true, :disable_after_rate => false, :cancel => true %>
</p>

<% if current_user.try(:admin?) %>
  <%= link_to 'Edit', edit_song_path(@song) %> |
<% end %>
<%= link_to 'Back', songs_path %>
